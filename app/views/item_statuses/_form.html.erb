<%= form_for(@item_status) do |f| %>
  <% if @item_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_status.errors.count, "error") %> prohibited this item_status from being saved:</h2>

      <ul>
      <% @item_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
